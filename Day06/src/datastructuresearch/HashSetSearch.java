package datastructuresearch;
import java.util.HashSet;

public class HashSetSearch {

    public static boolean search(HashSet<Integer> dataset, Integer value) {
        return dataset.contains(value);
    }
}
