package datastructuresearch;
import java.util.TreeSet;

public class TreeSetSearch {

    public static boolean search(TreeSet<Integer> dataset, Integer value) {
        return dataset.contains(value);
    }
}
